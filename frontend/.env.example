# Copie para ".env" (desenvolvimento local) ou configure como variável de
# ambiente no seu provedor de hospedagem (Vercel, Netlify etc.) para produção.

# URL pública do backend (sem barra no final). Em produção, use HTTPS.
VITE_API_BASE_URL=http://localhost:3333/api
